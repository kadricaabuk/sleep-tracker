import { IonContent } from '@ionic/react';
import React from 'react';

const Home = () => {
    return (
        <IonContent>
            <div>
                HOME
            </div>
        </IonContent>
    );
}

export default Home;
