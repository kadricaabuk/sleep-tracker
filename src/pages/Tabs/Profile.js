import { IonContent } from '@ionic/react';
import React from 'react';

const Profile = () => {
    return (
        <IonContent>
            Profile
        </IonContent>
    );
}

export default Profile;
